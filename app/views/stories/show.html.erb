<% unless current_user.nil? %>
<div id="toolbar">
  <%= link_to "Back to stories", stories_path %> | <%= link_to "Edit this story", edit_story_path(@story) %> | <%= link_to '❌', @story, data: { "turbo_method": :delete, "turbo-confirm": 'Are you sure you want to delete ' + @story.title.to_s + " ?" } %>
</div>
<% end %>
<%= render @story %>